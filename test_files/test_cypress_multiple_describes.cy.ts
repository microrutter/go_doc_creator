


describe("This is an amazing description thank you I am also the main test", function () {
    //constants (Main)
    const env = Cypress.env("HOST");
    const timestamp = Date.now();
    const pagerdutyToken = Cypress.env("");

    // The following is going to Test (Main)
    // this this and this good luck (Main)
  
    beforeEach("Setup constants and RegExp for tests (Main)", function () {
      cy.wait(5000)
        // Set Constants (Main)
          //More Constants for the before each (Main)
        
    });
  
    it("First test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });
  
    it("Second test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });

    it("Third test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });

    it("Fourth test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });

    it("Fith test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });

    it("Sixth test woohoo (Main)", function () {
      cy.wait(50000)
      // We are going top do this (Main)
        // Then we are going to do this (Main)
          // The we might do this (Main)
    });
  });

  describe("This is an amazing description tahnkl you", function () {
    //constants
    const env = Cypress.env("HOST");
    const timestamp = Date.now();
    const pagerdutyToken = Cypress.env("");

    // The following is going to Test 
    // this this and this good luck
  
    beforeEach("Setup constants and RegExp for tests", function () {
      cy.wait(5000)
        // Set Constants
          //More Constants for the before each
        
    });
  
    it("First test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });
  
    it("Second test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });

    it("Third test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });

    it("Fourth test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });

    it("Fith test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });

    it("Sixth test woohoo", function () {
      cy.wait(50000)
      // We are going top do this
        // Then we are going to do this
          // The we might do this
    });
  });